Assess whether this contract satisfies the requirements of {framework}
relevant to vendor agreements.

For each requirement listed below, return:
  - requirement: name of the requirement (use the exact name provided)
  - present: true if the contract clearly satisfies the requirement, false otherwise
  - evidence_quote: when present=true, the exact quote from the contract — verbatim
  - gap_description: when present=false, one sentence describing what is missing
                     or insufficient
  - severity: copy the severity from the requirement metadata (Low | Medium | High | Critical)

Set framework `passed` = true only if ALL Critical-severity requirements are
satisfied (i.e. all Critical checks have present=true).

Strict rules:
  - Use the exact requirement names provided.
  - Quote verbatim from the contract for evidence — do not paraphrase.
  - If a requirement is partially met, mark present=false and explain in gap_description.
  - If the framework is not relevant to this contract (e.g. HIPAA for an NDA with no
    PHI), mark applicable requirements as present=true with evidence "Not applicable —
    no PHI processed", and set passed=true.

Framework requirements (most relevant first):
---
{requirements_text}
---

Contract text:
---
{contract_text}
---
